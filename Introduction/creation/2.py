#with default values

#np.zeros()-- creates an array filled with zeros

#np.zeros(shape)-- shape is a tuple defining the dimensions of the array

#np.zeros(shape) ,(3) for 1d, (3, 3) for 2d, etc.

import numpy as np
zeroes_array = np.zeros(3)
print(zeroes_array)

